nano routes/order.py

Tambahkan fungsi baru ini **di bagian bawah** `routes/order.py`:


http://googleusercontent.com/immersive_entry_chip/1

### C. Update `app.py`

Anda harus mendaftarkan Blueprint `marketing_bp` di file utama `app.py`.

```bash
nano app.py


http://googleusercontent.com/immersive_entry_chip/2

## 🚀 Ringkasan

1.  **Eksekusi Perintah SQL** untuk membuat tabel `promotions` dan `reviews`.
2.  **Buat File `routes/marketing.py`** dan masukkan kode di atas.
3.  **Perbarui `routes/order.py`** dengan fungsi `get_order_history`.
4.  **Perbarui `app.py`** untuk mendaftarkan `marketing_bp`.
5.  **Restart Server Flask** (`Ctrl + C` lalu `python app.py`).

Sekarang *backend* Anda mendukung:
* Melihat daftar promosi.
* Melihat daftar riwayat pesanan (aktif, selesai, batal).

Langkah selanjutnya adalah finalisasi *endpoint* seperti **Driver Information (R)**, **Rating/Review (U)**, dan **Notifications (X)**. Apakah Anda ingin melanjutkan ke **Driver dan Rating/Review**?
